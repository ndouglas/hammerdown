use crate::prelude::*;

/// The total number of tiles in the map.
pub const NUMBER_OF_TILES: usize = (SCREEN_WIDTH * SCREEN_HEIGHT) as usize;

/// The maximum tile index.
pub const MAX_TILE_INDEX: usize = NUMBER_OF_TILES - 1;
